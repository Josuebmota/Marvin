# 1.6.1 — 2026-09-15

- **fix:** `--use=a --use=b` engolia a segunda flag (`find` pegava só a primeira). Agora aceita flag repetida e lista com vírgula. Achado ao ajustar dois projetos reais com graphify + ponytail no mesmo run — o ponytail ficou `não` sem aviso. Evidência: 210 verdes; `--dry-run --use=graphify --use=ponytail` registra os dois.
